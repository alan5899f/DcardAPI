# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DcardAPI' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DcardAPI

  target 'DcardAPITests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DcardAPIUITests' do
    # Pods for testing
  end

pod "Nuke"
pod "PKHUD" 

end
